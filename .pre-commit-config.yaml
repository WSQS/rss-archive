repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.1
    hooks:
      # Format first, then check — so check sees formatted output.
      - id: ruff-format
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
